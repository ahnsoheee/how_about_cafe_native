<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.springboot.howaboutcafe.mapper.ReviewMapper">

    <insert id="insertReview" parameterType="com.springboot.howaboutcafe.dto.ReviewDTO">
        INSERT INTO REVIEW(cafe_id, user_name, star, content) VALUES(#{cafe_id}, #{user_name}, #{star}, #{content}) 
    </insert>

    <select id="findByCafe" parameterType="int" resultType="com.springboot.howaboutcafe.dto.ReviewDTO">
        SELECT review_id, cafe_id, user_name, star, content, updated_at, created_at FROM REVIEW WHERE cafe_id=#{cafe_id} AND isDeleted=0 ORDER BY created_at DESC;
    </select>

    <update id="deleteReview" parameterType="int">
        UPDATE REVIEW SET isDeleted=1 WHERE review_id=#{review_id}
    </update>

</mapper>